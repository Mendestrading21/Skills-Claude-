import { fr } from './fr';

/**
 * The active copy dictionary. French is the only bundled locale for now, but
 * the shape is centralised so adding a locale is a drop-in.
 */
export const t = fr;

export type { Copy } from './fr';
export { fr };
